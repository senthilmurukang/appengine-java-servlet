ext {
    //used libraries versions
    libVersions =
            [
                    servletApi  : '3.1.0',
                    jettyServer : '9.4.30.v20200611'
            ]

    //used libraries
    libs = [
            servletApi  : "javax.servlet:javax.servlet-api:$libVersions.servletApi"
    ]
}